# Deprecated: v2-pre-ca-only

These files capture the old `EOA` and `AA/CA + ManagerForwardCall` workflow that existed before `ResonanceContract v2.0.0` switched to the CA-only permissionless-relay model.

Use this directory only when:

- a user pastes an old receipt and needs a legacy-path explanation
- the agent needs to explain why an `EOA` or `ManagerForwardCall` mental model no longer matches the current contract

Do not route new user-side writes through these deprecated documents.
